export let useLinks = false;
export let useMath = false;
export let useImages = false;

export function setUseLinks(value) {
    useLinks = value;
}

export function setUseMath(value) {
    useMath = value;
}

export function setUseImages(value) {
    useImages = value;
}

